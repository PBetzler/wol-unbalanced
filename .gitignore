# Downloaded third-party mods, reference data, and game files — never commit/republish
mods/

# Vendored third-party sources and build output
vendor/
build/
dist/

# Compiled tools (source lives in tools/)
tools/mpqpatch

# SC2 AI API self-verification venv (scripts/verify_api.py is tracked)
tools/sc2api/.venv/
# Runtime artifacts the harness / SC2 write (observation dump, replays)
tools/sc2api/*.json
*.SC2Replay

# Claude Code session/permission settings — never publish
.claude/

# OS / Python noise
.DS_Store
__pycache__/
*.pyc
